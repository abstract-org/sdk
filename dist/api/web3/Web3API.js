export default class Web3API {
    constructor(config) { }
    createQuest(name, description) {
        // alternate implementation details
        return true;
    }
    createPool(name, description) {
        // alternate implementation details
        return true;
    }
    citeQuest(questId, userId) {
        // alternate implementation details
        return true;
    }
}
//# sourceMappingURL=Web3API.js.map